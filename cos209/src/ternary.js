// Concept: Ternary operators are a shorthand for simple if-else conditions.
const age = 17;
const canVote = age >= 18 ? 'yes' : 'no';
console.log('canVote:', canVote);

const score = 85;
const grade = score >= 90 ? 'A' : score >= 80 ? 'B' : 'C';
console.log('grade:', grade);
